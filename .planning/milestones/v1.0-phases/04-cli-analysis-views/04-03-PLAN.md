---
phase: 04-cli-analysis-views
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - pipeline/cli/main.py
autonomous: false
requirements: [CLI-01, CLI-02, CLI-03]
must_haves:
  truths:
    - "pipeline collect --help exits 0 and lists --region, --start, --data-type options"
    - "pipeline export --help exits 0 and lists --output option"
    - "pipeline status with empty DB prints 'No data collected yet'"
    - "_resolve_regions('seoul') returns only SEOUL_REGIONS (25 entries, all LAWD_CDs start '11')"
    - "_resolve_regions('unknown') raises SystemExit(1)"
    - "pipeline export writes a UTF-8 BOM CSV (first 3 bytes = 0xEF 0xBB 0xBF)"
  artifacts:
    - path: "pipeline/cli/main.py"
      provides: "Complete Typer CLI with collect/export/status commands and _resolve_regions"
      exports: ["app", "collect", "export", "status", "_resolve_regions"]
      contains: "utf-8-sig"
  key_links:
    - from: "pipeline/cli/main.py:export"
      to: "apartment_analysis VIEW"
      via: "pd.read_sql('SELECT * FROM apartment_analysis', conn)"
      pattern: "apartment_analysis"
    - from: "pipeline/cli/main.py:collect"
      to: "pipeline.collectors.trade_rent.collect_all_regions"
      via: "plain def (not async def) — asyncio.run() is internal to collector"
      pattern: "def collect"
    - from: "pyproject.toml"
      to: "pipeline.cli.main:app"
      via: "[project.scripts] pipeline entry point"
      pattern: "pipeline.*pipeline.cli.main:app"
---

<objective>
Verify and finalize the Typer CLI. The stub from Plan 01 already contains the full implementation. This plan runs the remaining tests, performs a human checkpoint on the installed CLI, and produces the final phase summary.

Purpose: Confirm all 12 Phase 4 tests pass green, the installed `pipeline` console script works end-to-end, and the CSV export produces a BOM-prefixed UTF-8 file that Excel can open without Korean character garbling.
Output: All 12 tests green. `pipeline --help` works from the terminal. Phase 4 complete.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cli-analysis-views/04-RESEARCH.md
@.planning/phases/04-cli-analysis-views/04-01-SUMMARY.md
@.planning/phases/04-cli-analysis-views/04-02-SUMMARY.md

<interfaces>
<!-- Contracts that must hold after this plan. -->

pipeline/cli/main.py must expose:
  app = typer.Typer(no_args_is_help=True)
  def _resolve_regions(region: Optional[str]) -> dict[str, str]
  @app.command() def collect(region, start, data_type)
  @app.command() def export(output, query)
  @app.command() def status()

pyproject.toml must contain:
  [project.scripts]
  pipeline = "pipeline.cli.main:app"

  [project.dependencies]
  "typer>=0.12.0"
  "pandas>=2.0"

Region resolution rules:
  None          -> PIPELINE_REGIONS (29 entries)
  "seoul"       -> SEOUL_REGIONS (25 entries, v.startswith("11"))
  "gyeonggi"    -> GYEONGGI_REGIONS (4 entries, v.startswith("41"))
  Korean name   -> {name: PIPELINE_REGIONS[name]} single-entry dict
  unknown str   -> typer.echo(err=True) + raise typer.Exit(1)

export command:
  pd.read_sql(sql, conn) where conn is sqlite3.Connection from init_db(DB_PATH)
  df.to_csv(output, encoding="utf-8-sig", index=False)
  First 3 bytes of output file must be b'\xef\xbb\xbf'

collect command:
  Must be plain def (NOT async def) — asyncio.run() is inside the collectors
  check MOLIT_API_KEY before dispatching trade/rent/building
  check TMAP_APP_KEY before dispatching subway
  print clear error and continue (do NOT crash) when keys are missing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Phase 4 test suite and fix any remaining failures</name>
  <files>pipeline/cli/main.py</files>
  <read_first>
    - tests/test_pipeline_phase4.py (all 12 tests — understand what each asserts)
    - pipeline/cli/main.py (current state from Plan 01 — check for any issues)
    - pipeline/storage/schema.py (confirm create_views is present after Plan 02)
  </read_first>
  <action>
Run the full test suite and diagnose any failures:

```bash
pytest tests/test_pipeline_phase4.py -v -q
```

Common failure modes and fixes:

1. test_status_empty fails with TypeError or wrong output:
   The test monkey-patches `pipeline.cli.main.init_db`. If the status() function is calling
   `init_db(DB_PATH)` with the module-level import, the patch must replace `pipeline.cli.main.init_db`,
   not `pipeline.storage.schema.init_db`. Verify the import in main.py is:
   `from pipeline.storage.schema import DB_PATH, init_db`
   This means the test can patch `pipeline.cli.main.init_db` directly.

2. test_export_utf8_sig or test_export_bom_present fails:
   Confirm pipeline/cli/main.py export() uses `df.to_csv(output, encoding="utf-8-sig", index=False)`.
   The string must be exactly `"utf-8-sig"` (hyphen, not underscore).

3. test_resolve_regions_invalid fails (no SystemExit raised):
   Confirm _resolve_regions() calls `raise typer.Exit(1)` (not `sys.exit(1)` or `return`).
   typer.Exit inherits from SystemExit, so `pytest.raises(SystemExit)` will catch it.

4. Any create_views ImportError:
   Means Plan 02 did not complete. Run Plan 02 first, then re-run this plan.

After fixing any issues, run:
```bash
pytest tests/test_pipeline_phase4.py -v
pytest tests/ -q
```

Both must exit 0 with all tests passing.

Also install the CLI entry point and verify it works:
```bash
pip install -e . -q
pipeline --help
pipeline status
```
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_phase4.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 12 tests in tests/test_pipeline_phase4.py pass (0 failures, 0 errors)
    - Full test suite (tests/) passes with 0 failures
    - `pipeline --help` exits 0 and shows collect/export/status subcommands
    - `pipeline status` exits 0 and prints either "No data collected yet" or a table
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Typer CLI with collect/export/status subcommands, apartment_analysis SQLite VIEW,
    and UTF-8 BOM CSV export — all 12 automated tests passing.
  </what-built>
  <how-to-verify>
1. Verify the installed CLI entry point:
   ```
   pipeline --help
   ```
   Expected: Shows "부동산 데이터 수집 파이프라인" help with collect/export/status listed.

2. Check status with the real database:
   ```
   pipeline status
   ```
   Expected: Either "No data collected yet" (if realestate.db is empty) OR a table of regions,
   data_types, last_ym, and record counts. No errors.

3. Test the export command (generates a CSV from whatever is in realestate.db):
   ```
   pipeline export --output test_export.csv
   ```
   Then verify the BOM:
   ```
   python -c "f=open('test_export.csv','rb'); print(f.read(3))"
   ```
   Expected: b'\xef\xbb\xbf'

4. Open test_export.csv in Excel (or verify with head -1 in a UTF-8 capable terminal)
   and confirm Korean column names/values are not garbled.

5. Run the full test suite one final time:
   ```
   pytest tests/ -q
   ```
   Expected: All tests pass, 0 failures.
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
```bash
# All 12 phase 4 tests green
pytest tests/test_pipeline_phase4.py -v

# Full suite clean
pytest tests/ -q

# CLI entry point works
pipeline --help

# VIEW exists in real DB (if it exists)
python -c "
import os
if os.path.exists('realestate.db'):
    from pipeline.storage.schema import init_db
    conn = init_db('realestate.db')
    row = conn.execute(\"SELECT COUNT(*) FROM apartment_analysis\").fetchone()
    print('apartment_analysis rows:', row[0])
else:
    print('realestate.db not found — run pipeline collect first')
"
```
</verification>

<success_criteria>
- All 12 tests/test_pipeline_phase4.py tests pass
- tests/ full suite passes (no regressions in phases 1-3 tests)
- `pipeline --help` shows collect / export / status
- `pipeline export --output x.csv` produces a file starting with BOM bytes b'\xef\xbb\xbf'
- `pipeline status` exits 0
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli-analysis-views/04-03-SUMMARY.md`
</output>
