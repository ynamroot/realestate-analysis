---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - pipeline/config/regions.py
autonomous: true
requirements:
  - FOUND-03

must_haves:
  truths:
    - "PIPELINE_REGIONS contains exactly 29 entries (25 Seoul + 4 Gyeonggi)"
    - "All LAWD_CD values are exactly 5 digits and numeric"
    - "All 4 required Gyeonggi codes present: 41175, 41390, 41550, 41220"
    - "SEOUL_REGIONS and GYEONGGI_REGIONS convenience dicts are available"
  artifacts:
    - path: "pipeline/config/regions.py"
      provides: "PIPELINE_REGIONS dict with 29 verified LAWD_CD mappings"
      exports: ["PIPELINE_REGIONS", "SEOUL_REGIONS", "GYEONGGI_REGIONS"]
      contains: "PIPELINE_REGIONS"
  key_links:
    - from: "pipeline/config/regions.py"
      to: "app/data/region_codes.py"
      via: "LAWD_CD values copied and verified from SEOUL_SIGUNGU + GYEONGGI_SIGUNGU"
      pattern: "\"41175\""
---

<objective>
Implement the PIPELINE_REGIONS mapping — a hardcoded dict of 29 LAWD_CDs for the 5 pipeline target regions (Seoul 25 + Gyeonggi 4). Also provides SEOUL_REGIONS and GYEONGGI_REGIONS convenience subsets.

Purpose: Turns the two FOUND-03 test stubs (test_lawd_cd_format and test_required_regions_present) GREEN. This module is the single source of truth for which districts the pipeline collects.
Output: pipeline/config/regions.py — importable as `from pipeline.config.regions import PIPELINE_REGIONS`.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- LAWD_CDs verified in two independent sources: realestate_csv.py SIGUNGU_MAP and app/data/region_codes.py GYEONGGI_SIGUNGU + SEOUL_SIGUNGU -->
<!-- Copy from 01-RESEARCH.md Pattern 4 verbatim -->

Seoul 25 districts (all start with "11"):
  강남구:   11680   강동구:   11740   강북구:   11305   강서구:   11500
  관악구:   11620   광진구:   11215   구로구:   11530   금천구:   11545
  노원구:   11350   도봉구:   11320   동대문구: 11230   동작구:   11590
  마포구:   11440   서대문구: 11410   서초구:   11650   성동구:   11200
  성북구:   11290   송파구:   11710   양천구:   11470   영등포구: 11560
  용산구:   11170   은평구:   11380   종로구:   11110   중구:     11140
  중랑구:   11260

Gyeonggi 4 regions (all start with "41"):
  성남시 분당구:  41175   (위례 일부 포함 지역)
  과천시:         41390
  하남시:         41550   (위례신도시 행정구역)
  안양시 동안구:  41220   (인덕원 행정구역)

Total: 29 entries

pipeline/config/__init__.py is created by Plan 01-02 Task 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pipeline/config/regions.py</name>
  <files>pipeline/config/regions.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4 — PIPELINE_REGIONS dict verbatim)
    - app/data/region_codes.py (cross-check: SEOUL_SIGUNGU and GYEONGGI_SIGUNGU for verification of codes)
    - tests/test_pipeline_foundation.py (test_lawd_cd_format and test_required_regions_present — the exact assertions)
  </read_first>
  <behavior>
    - test_lawd_cd_format: for every (name, code) in PIPELINE_REGIONS.items(), len(code)==5 and code.isdigit() must be True
    - test_required_regions_present: set(PIPELINE_REGIONS.values()) must contain all 29 required codes; keys must include substrings "분당", "과천", "하남", "동안"
  </behavior>
  <action>
Create pipeline/config/regions.py with exactly this content (from 01-RESEARCH.md Pattern 4):

```python
"""
Target region LAWD_CD mapping for the real estate pipeline.

All codes are 5-digit 시군구-level codes as required by MOLIT RTMSOBJSvc endpoints.
Do NOT use 2-digit 시도 codes or 10-digit 법정동 codes — they return 0 results silently.

Sources (both verified):
  - realestate_csv.py SIGUNGU_MAP
  - app/data/region_codes.py SEOUL_SIGUNGU + GYEONGGI_SIGUNGU
"""

PIPELINE_REGIONS: dict[str, str] = {
    # 서울특별시 25개 자치구
    "강남구":     "11680",
    "강동구":     "11740",
    "강북구":     "11305",
    "강서구":     "11500",
    "관악구":     "11620",
    "광진구":     "11215",
    "구로구":     "11530",
    "금천구":     "11545",
    "노원구":     "11350",
    "도봉구":     "11320",
    "동대문구":   "11230",
    "동작구":     "11590",
    "마포구":     "11440",
    "서대문구":   "11410",
    "서초구":     "11650",
    "성동구":     "11200",
    "성북구":     "11290",
    "송파구":     "11710",
    "양천구":     "11470",
    "영등포구":   "11560",
    "용산구":     "11170",
    "은평구":     "11380",
    "종로구":     "11110",
    "중구":       "11140",
    "중랑구":     "11260",
    # 경기도 4개 권역
    "성남시 분당구":  "41175",   # 위례 일부 포함 지역
    "과천시":         "41390",
    "하남시":         "41550",   # 위례신도시 행정구역
    "안양시 동안구":  "41220",   # 인덕원 행정구역
}

# Convenience subsets
SEOUL_REGIONS: dict[str, str] = {
    k: v for k, v in PIPELINE_REGIONS.items() if v.startswith("11")
}

GYEONGGI_REGIONS: dict[str, str] = {
    k: v for k, v in PIPELINE_REGIONS.items() if v.startswith("41")
}
```

Verify after writing:
- len(PIPELINE_REGIONS) == 29
- len(SEOUL_REGIONS) == 25
- len(GYEONGGI_REGIONS) == 4
- All codes pass: assert all(len(v) == 5 and v.isdigit() for v in PIPELINE_REGIONS.values())
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_foundation.py::test_lawd_cd_format tests/test_pipeline_foundation.py::test_required_regions_present -x -q</automated>
  </verify>
  <done>
    - pipeline/config/regions.py exists
    - `pytest tests/test_pipeline_foundation.py::test_lawd_cd_format tests/test_pipeline_foundation.py::test_required_regions_present -x -q` exits 0 (GREEN)
    - `python -c "from pipeline.config.regions import PIPELINE_REGIONS; assert len(PIPELINE_REGIONS)==29"` exits 0
  </done>
  <acceptance_criteria>
    - grep "PIPELINE_REGIONS" pipeline/config/regions.py exits 0
    - grep "SEOUL_REGIONS" pipeline/config/regions.py exits 0
    - grep "GYEONGGI_REGIONS" pipeline/config/regions.py exits 0
    - grep '"41175"' pipeline/config/regions.py exits 0
    - grep '"41390"' pipeline/config/regions.py exits 0
    - grep '"41550"' pipeline/config/regions.py exits 0
    - grep '"41220"' pipeline/config/regions.py exits 0
    - python -c "from pipeline.config.regions import PIPELINE_REGIONS; assert len(PIPELINE_REGIONS)==29, len(PIPELINE_REGIONS)" exits 0
    - python -c "from pipeline.config.regions import PIPELINE_REGIONS; assert all(len(v)==5 and v.isdigit() for v in PIPELINE_REGIONS.values())" exits 0
    - pytest tests/test_pipeline_foundation.py::test_lawd_cd_format -x -q exits 0
    - pytest tests/test_pipeline_foundation.py::test_required_regions_present -x -q exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
After task completes:
- `pytest tests/test_pipeline_foundation.py::test_lawd_cd_format tests/test_pipeline_foundation.py::test_required_regions_present -x -q` — both GREEN
- `python -c "from pipeline.config.regions import PIPELINE_REGIONS, SEOUL_REGIONS, GYEONGGI_REGIONS; print(len(PIPELINE_REGIONS), len(SEOUL_REGIONS), len(GYEONGGI_REGIONS))"` — prints "29 25 4"
</verification>

<success_criteria>
Plan 03 is complete when:
1. `pytest tests/test_pipeline_foundation.py::test_lawd_cd_format -x -q` GREEN
2. `pytest tests/test_pipeline_foundation.py::test_required_regions_present -x -q` GREEN
3. `python -c "from pipeline.config.regions import PIPELINE_REGIONS; assert len(PIPELINE_REGIONS)==29"` exits 0
4. All 29 LAWD_CD values are 5-digit numeric strings (verified by test)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
