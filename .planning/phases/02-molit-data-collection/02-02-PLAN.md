---
phase: 02-molit-data-collection
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - pipeline/processors/normalizer.py
autonomous: true
requirements: [PRICE-03, PRICE-04]

must_haves:
  truths:
    - "normalize_trade_item strips commas from dealAmount and returns an int"
    - "normalize_trade_item converts excluUseAr to float"
    - "normalize_trade_item zero-pads dealMonth '1' to '01'"
    - "normalize_rent_item strips commas from deposit and returns an int"
    - "aggregate_monthly groups items by (apt_nm, umd_nm, deal_ym, exclu_use_ar) and computes min/max/avg/count"
    - "get_month_range starts at the given YYYYMM and ends at the previous calendar month"
  artifacts:
    - path: "pipeline/processors/normalizer.py"
      provides: "Pure normalization and aggregation functions — no I/O, no DB calls"
      exports: ["normalize_trade_item", "normalize_rent_item", "aggregate_monthly", "get_month_range", "_safe_int", "_safe_float"]
      min_lines: 80
  key_links:
    - from: "pipeline/collectors/trade_rent.py"
      to: "pipeline/processors/normalizer.py"
      via: "from pipeline.processors.normalizer import normalize_trade_item, normalize_rent_item, aggregate_monthly, get_month_range"
      pattern: "from pipeline.processors.normalizer import"
---

<objective>
Implement `pipeline/processors/normalizer.py` — all pure normalization and aggregation functions with no I/O or DB dependencies.

Purpose: The normalizer is the data quality layer for Phase 2. It converts raw MOLIT XML dict fields to typed Python values (PRICE-04) and groups normalized items into monthly aggregate rows (PRICE-03). Being pure functions they are trivially testable and can be developed independently of storage (Plan 03).

Output: `pipeline/processors/normalizer.py` with 6 exported functions. All 7 related test stubs from Plan 01 turn green.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-molit-data-collection/02-RESEARCH.md

<interfaces>
<!-- Source patterns — copy verbatim, do not deviate -->

From 02-RESEARCH.md Pattern 1 (get_month_range):
```python
from datetime import datetime, timedelta

def get_month_range(start_ym: str = "200601") -> list[str]:
    """Return all YYYYMM strings from start_ym up to previous calendar month."""
    now = datetime.now()
    prev = now.replace(day=1) - timedelta(days=1)
    end_y, end_m = prev.year, prev.month
    sy, sm = int(start_ym[:4]), int(start_ym[4:6])
    result = []
    y, m = sy, sm
    while (y, m) <= (end_y, end_m):
        result.append(f"{y}{m:02d}")
        m += 1
        if m > 12:
            m, y = 1, y + 1
    return result
```

From 02-RESEARCH.md Pattern 2 (normalize_trade_item / normalize_rent_item):
- dealAmount: item.get("dealAmount","").replace(",","").strip() → _safe_int()
- excluUseAr: item.get("excluUseAr","").strip() → _safe_float()
- dealMonth: item.get("dealMonth","").strip().zfill(2)
- deal_ym: f"{item.get('dealYear','')}{deal_month_raw.zfill(2)}"

From 02-RESEARCH.md Pattern 3 (aggregate_monthly):
- Group by key: (apt_nm, umd_nm, deal_ym, exclu_use_ar)
- For deal_type="trade": price_min, price_max, price_avg from item["price"]
- For deal_type="rent": deposit_min, deposit_max, deposit_avg from item["deposit"]
- Null out the other set (trade rows have deposit_*=None, rent rows have price_*=None)

MOLIT field names (camelCase, all strings from XML):
- Trade: aptNm, umdNm, jibun, roadNm, dealAmount, excluUseAr, dealYear, dealMonth, buildYear, floor
- Rent adds: deposit, monthlyRent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pipeline/processors/normalizer.py</name>
  <files>pipeline/processors/normalizer.py</files>
  <read_first>
    - pipeline/processors/__init__.py (confirm package exists)
    - .planning/phases/02-molit-data-collection/02-RESEARCH.md (Pattern 1, 2, 3 — copy implementations verbatim)
    - tests/test_pipeline_phase2.py (read all 7 normalizer-related test assertions to verify implementation matches)
  </read_first>
  <behavior>
    - test_normalize_trade_deal_amount: normalize_trade_item({"dealAmount": "55,000", ...})["price"] == 55000 (int)
    - test_normalize_trade_exclu_use_ar: normalize_trade_item({"excluUseAr": "84.9700", ...})["exclu_use_ar"] is a float close to 84.97
    - test_normalize_trade_deal_month_padding: normalize_trade_item({"dealMonth": "1", "dealYear": "2024", ...})["deal_month"] == "01" and ["deal_ym"] == "202401"
    - test_normalize_rent_deposit: normalize_rent_item({"deposit": "30,000", ...})["deposit"] == 30000 (int)
    - test_aggregate_monthly_grouping: two items with same key collapse to len==1, deal_count==2
    - test_aggregate_monthly_stats: price_min=55000, price_max=57000, price_avg=56000.0
    - test_month_range_bounds: months[0]=="200601", months[-1]==previous_calendar_month, current_month NOT in months
  </behavior>
  <action>
Create `pipeline/processors/normalizer.py` with the following exact content — copy the implementations from RESEARCH.md Patterns 1, 2, 3 verbatim:

Module docstring: "Pure normalization and aggregation functions for MOLIT API responses. No I/O, no DB calls — all functions accept/return plain Python types."

Implement these 6 functions in order:

1. `_safe_int(s: str) -> int | None` — try int(s.strip()), return None on ValueError or empty
2. `_safe_float(s: str) -> float | None` — try float(s.strip()), return None on ValueError or empty
3. `get_month_range(start_ym: str = "200601") -> list[str]` — from RESEARCH Pattern 1 exactly
4. `normalize_trade_item(item: dict) -> dict` — from RESEARCH Pattern 2 exactly. Returns dict with keys: apt_nm, umd_nm, jibun, road_nm, build_year (int|None), deal_year (int|None), deal_month (str, zero-padded), deal_ym (str), exclu_use_ar (float|None), price (int|None), floor (int|None)
5. `normalize_rent_item(item: dict) -> dict` — from RESEARCH Pattern 2 exactly. Returns same keys as trade plus: deposit (int|None), monthly_rent (int|None). No "price" key (rent has no sale price).
6. `aggregate_monthly(normalized_items: list[dict], deal_type: str) -> list[dict]` — from RESEARCH Pattern 3 exactly. Groups by (apt_nm, umd_nm, deal_ym, exclu_use_ar). For "trade": price_min/max/avg, deposit_*=None. For "rent": deposit_min/max/avg, price_*=None. Always includes: deal_count, deal_type.

Critical implementation notes:
- dealAmount and deposit BOTH contain commas in raw MOLIT data. Use .replace(",", "") before int()
- monthlyRent may also contain commas. Use .replace(",", "") before _safe_int()
- dealMonth "1" must become "01" — use .zfill(2) after stripping whitespace
- deal_ym must be constructed as f"{dealYear}{dealMonth.zfill(2)}" — both fields from the SAME item
- aggregate_monthly key is a 4-tuple — items with different exclu_use_ar are separate groups even if same apartment name
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_phase2.py::test_normalize_trade_deal_amount tests/test_pipeline_phase2.py::test_normalize_trade_exclu_use_ar tests/test_pipeline_phase2.py::test_normalize_trade_deal_month_padding tests/test_pipeline_phase2.py::test_normalize_rent_deposit tests/test_pipeline_phase2.py::test_aggregate_monthly_grouping tests/test_pipeline_phase2.py::test_aggregate_monthly_stats tests/test_pipeline_phase2.py::test_month_range_bounds -v</automated>
  </verify>
  <done>All 7 normalizer tests pass (show as PASSED, not XFAIL). `pytest tests/ -q` exits 0.</done>
</task>

</tasks>

<verification>
1. `pytest tests/test_pipeline_phase2.py::test_normalize_trade_deal_amount tests/test_pipeline_phase2.py::test_normalize_trade_exclu_use_ar tests/test_pipeline_phase2.py::test_normalize_trade_deal_month_padding tests/test_pipeline_phase2.py::test_normalize_rent_deposit tests/test_pipeline_phase2.py::test_aggregate_monthly_grouping tests/test_pipeline_phase2.py::test_aggregate_monthly_stats tests/test_pipeline_phase2.py::test_month_range_bounds -v` — all 7 PASSED
2. `grep -E "def normalize_trade_item|def normalize_rent_item|def aggregate_monthly|def get_month_range" pipeline/processors/normalizer.py` returns 4 lines
3. `pytest tests/ -q` exits 0 (full suite still green)
</verification>

<success_criteria>
- pipeline/processors/normalizer.py exists with 6 functions exported
- All 7 normalizer test stubs from Plan 01 are PASSED (not XFAIL)
- No test regressions in the full suite
</success_criteria>

<output>
After completion, create `.planning/phases/02-molit-data-collection/02-02-SUMMARY.md`
</output>
